# GPU 生产部署：与 docker-compose.prod.yml 叠加使用（同一镜像，仅增加 NVIDIA 设备预留）
# 前置：安装 nvidia-container-toolkit

services:
  picture-embedding:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
